name: home-stuff
runtime:
  name: nodejs
  options:
    nodeargs: "--loader ts-node/esm --no-warnings --es-module-specifier-resolution=node"
description: Bennett's home infrastructure
backend:
  url: s3://home-stuff-bennettp123
